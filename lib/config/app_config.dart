class AppConfig {
  /// Master switch for your project:
  /// - Set to [true] when testing with your 3 real clients (Live Production).
  /// - Set to [false] right before your university viva for instant sandbox demos.
  static const bool isLiveProduction = true;
}
